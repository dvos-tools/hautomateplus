# Home Assistant Configuration
# Replace these values with your actual Home Assistant instance details

# Home Assistant URL (with protocol and port if needed)
# Example: ws://192.168.1.100:8123/api/websocket or wss://your-ha-instance.com/api/websocket
HA_URL= wss://192.168.1.100:8123/api/websocket

# Home Assistant Long-Lived Access Token
# Generate this in Home Assistant: Profile > Long-Lived Access Tokens
HA_ACCESS_TOKEN=your_long_lived_access_token_here

# Optional: Environment
NODE_ENV=production

# Optional: Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Optional: Heartbeat interval in milliseconds (default: 30000)
HEARTBEAT_INTERVAL=30000

# Optional: Max reconnect attempts (default: 10)
MAX_RECONNECT_ATTEMPTS=10

# Optional: Base reconnect delay in milliseconds (default: 1000)
BASE_RECONNECT_DELAY=1000

# Optional: Max reconnect delay in milliseconds (default: 30000)
MAX_RECONNECT_DELAY=30000
